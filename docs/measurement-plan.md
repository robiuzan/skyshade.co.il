# Measurement plan & change log

**The rule (merge requirement):** no SEO/CRO recommendation is "done" until its metric is
named and its baseline value is written down here; every shipped change gets a dated
entry below. This defends against the audit's biggest failure mode — simultaneous change
making everything unattributable.

## Baseline (captured 2026-08-17, BEFORE any of this deployed)

> **These bullets are frozen history, not current state.** Most have since been fixed — AI
> crawlers now return 200, www now 301s to the apex, the managed robots.txt is off, DMARC is
> live, and the GSC property is verified. **Never update a value here**; the whole point of the
> section is to preserve what "before" looked like. Current state lives in the change log below.

- Per-page titles/descriptions/word counts: `baseline-seo-snapshot.json` (repo root)
- GA4 (G-BRZ0S93NFS): page views only — the emptiness is the baseline
- AI crawler access: GPTBot / PerplexityBot / ClaudeBot → **HTTP 403** (curl-verified)
- www.skyshade.co.il → **200 mirror**, no redirect
- Live robots.txt: Cloudflare managed content-signals version, NOT the repo's Allow-all
- DMARC: no `_dmarc` record
- GSC: never verified (no historical data exists)

## Waves (non-negotiable order)

1. **Week 1:** GA4 event tags + GSC verification → then 4 clean weeks of lead/traffic baseline
2. **Weeks 5–6:** Consent Mode v2 + banner — write down the expected 20–40% measured-session drop first
3. **Week 7+:** GTM defer experiment
4. Title changes freeze for 90 days after the Phase-1 final state (GSC CTR deltas need stability)

## KPI set

Leads/week by channel (`generate_lead` + `phone_call_click` + `whatsapp_click` — intent
proxy, overcounts, say so) · form_start→submit rate per template · GSC clicks + position
on the fixed ~30-keyword set · indexed-page count · `lead_submit_failed` (alarm if >0) ·
AI-assistant-channel sessions.

## Change log

| Date | Change | Commit | Expected effect / metric |
|---|---|---|---|
| 2026-09-03 | **Sitemap verified end-to-end against live, ahead of submission.** `/sitemap.xml` checked on every dimension Google applies: `200` + `application/xml`; well-formed, correct `sitemaps.org/0.9` namespace; 2,372 bytes / 35 URLs (limits 50 MB / 50,000); **35/35 `<loc>` return `200`** with no redirect hops; every `<loc>` matches that page's `rel="canonical"` byte-for-byte; all trailing-slashed per `trailingSlash: true`; 36 exported pages vs 35 listed, the sole omission `/404/` by design; no `noindex` in the set; `Sitemap:` present in live `robots.txt`; live file byte-identical to `out/sitemap.xml` (no deploy drift). **No code change was needed** — `app/sitemap.ts` and the `postbuild` guard were already correct. Two non-blocking findings recorded: the GSC verification file `google2bc4e3517c114dd1.html` **308-redirects** to its extensionless URL (Cloudflare Pages strips `.html`) before serving the right body at `200` — if the property ever reads as unverified, that is why, and DNS TXT is the fix; and the 16 city pages measure **71.3% mean pairwise 8-gram overlap** at 364–372 words each, still the doorway pattern of CLAUDE.md rule 3 | (no code change) | **The submission itself is still PENDING** — a manual step in the owner's Google account (plan 1.4 / sprint 1.8). Google retired the `ping?sitemap=` endpoint in June 2023, so there is no scripted path. Once submitted: indexed-page count against the 35 submitted, and how many of the 16 city URLs land in "Crawled – currently not indexed" — read that as confirmation of the known duplication, not a new defect |
| 2026-09-01 | **Pre-baseline `LeadForm` batch** (adversarially-verified audit: 44 findings raised, 26 survived, 18 refuted). Shipped together, deliberately **before** the GTM tags exist: success state `role="status"` + focus restore + `aria-busy` (SC 4.1.3); per-field validation with focus move and `aria-invalid` replacing one generic message for two fields; a **9-digit phone floor** so `"0"` can no longer become a counted lead; `placeholder:text-gray-400 → 500` (SC **1.4.3** — a live inaccuracy against the 2.0 AA claim on `/accessibility/`, ≈2.5:1 → ≈4.7:1); `data-cta` on the 4 conversion links that had none (`form-whatsapp`, `form-recovery-whatsapp`, `form-recovery-call`, `footer-email`); phone field `text-start → text-end` (a logical-properties sweep had silently flipped it left because the element is `dir="ltr"`); mirrored Send icon; Hebrew `ב` proclitic binding. **JS 119.3 → 119.5 KB gz (+197 B), inside the 120 KB budget.** Typecheck, lint, build and sitemap guard all pass | (this batch) | **The point of the timing:** the phone floor changes the `generate_lead` numerator and the `data-cta` additions change `link_location` cardinality — both would be a visible step change if they landed mid-window, and GA4 cannot re-attribute retroactively. Landing them now means the four clean weeks measure **one** consistent behaviour. Baseline for all of it: the tags do not exist yet, so every value is still zero |
| 2026-09-01 | **Documentation environment built out**: `docs/README.md` (the map + single-owner rule), `accessibility-and-i18n.md`, `mobile-ux-and-personalization.md`, `conversion-funnel.md`, `performance-budgets.md`, `data-tracking-infrastructure.md`, `sprint-roadmap.md`, plus the `accessibility-wcag` skill. **Two live defects found in the process:** GA4 Enhanced Measurement would capture the visitor's name and phone from the WhatsApp recovery link's `?text=` (P0, dashboard fix, logged as sprint item 1.2c), and `gtm-tag-spec.md` listed 2 of the 4 real `form_location` values — a funnel dashboard built from it would have charted 2 of the 24 pages carrying the form | (this batch) | No traffic effect. Prevents a PII leak into GA4 and an under-scoped GA4 build. Also records the measured JS budget (119.5/120 KB) so the next client component is a deliberate decision |
| 2026-08-25 | **GTM container inspected live — W1 is confirmed NOT started, and the container is fleet-shared.** `GTM-KWGGH438` serves ten IL sites via a hostname→GA4 lookup and holds exactly one real tag (the GA4 config). None of the six spec events exist; no `tel:`/`wa.me` click triggers. `generate_lead` appears in the file only as GA4 library boilerplate — not a configured tag. `docs/gtm-tag-spec.md` corrected: every trigger now requires a `Page Hostname equals skyshade.co.il` condition, or it would fire on nine other clients' sites | (this batch) | Prevents cross-client analytics contamination; the four-week baseline still has not started |
| 2026-08-25 | **All six service pages deepened** with per-product decision/material sections (`ServiceSection` in `lib/content.ts`) — pergola types and what drives the design, fence spacing vs wind load and gate decisions, cladding substructure and the three board materials, deck sub-frame and poolside, outdoor-kitchen infrastructure sequencing, balcony-closure types incl. the honest downsides. Deliberately different content per page, not a shared template. Measured before → after: cross-service 8-gram overlap **57% → 42%**; unique words per page **366–521 → 547–973**; `/service/pergolas/` 670 → 1,122 words | (this batch) | GSC position on the six head terms, especially `פרגולות אלומיניום`. Baseline: the pages were ~370 unique words with 57% shared boilerplate |
| 2026-08-25 | **Entity corroboration landed** — owner supplied the live Google Business Profile, Facebook and Instagram. Wired into `schema.sameAs` + `hasMap` and into the footer as icon links on all 36 pages, from one list in `lib/site-config.ts` so markup and UI cannot drift. Profiles live in CODE, not `site.config.json` (hub-synced, would be overwritten) | (this batch) | Entity resolution: brand search `סקיי שייד` returning this business rather than Skyroof/Skylite; knowledge-panel eligibility; assistants naming the business correctly. **Baseline 2026-08-17: `sameAs: []`, no knowledge panel, brand SERP lost to competitors.** Re-check in the Oct review |
| 2026-08-25 | **Lead form added to the 6 service pages and 16 city pages** — coverage went from 2 of 36 pages to 24. Service pages pre-select their own service; city pages get a compact variant with no free-text field. Field ids are now `useId`-scoped so two forms can share a page without breaking `<label for>` | (this batch) | `form_start` → `generate_lead` rate **per `form_location`** (`service-page`, `city-page` vs the existing `home-hero`, `contact-page`). Baseline: those two locations did not exist. **Only readable once the GTM tags are built** |
| 2026-08-25 | **`/guides/pergola-sukkah/` shipped** — the one roadmap item with a hard deadline (Sukkot is late Sept; the roadmap said publish by mid-Elul, which is now). Needed no owner facts. Framed as מותאמת with an explicit "we do not certify kashrut" block; halachic questions referred to the reader's rabbi; zero price claims. Verified: 5/5 FAQ answers verbatim in the rendered page | (this batch) | GSC impressions/clicks on `פרגולה לסוכה`-family queries through Sept–Oct. **Baseline: zero** — no page targeted this. Judge it in the Oct review, then again next Elul |
| 2026-08-24 | **Backfill — GSC property verification, which happened but was never logged.** Recorded now because the missing row is what made the frozen baseline bullet "GSC: never verified" (accurate as of 2026-08-17) read as a contradiction of CLAUDE.md §9's "Search Console **verified**". Neither statement was wrong; the event between them was simply absent from this log. Date evidence: `a1b35ce` is the commit where CLAUDE.md first states the property is verified. Method is the HTML file `public/google2bc4e3517c114dd1.html` (URL-prefix property) — which is what rule 8's never-delete protects | `a1b35ce` (doc evidence) | No traffic effect. Dates the start of GSC data availability and closes the gap that made two docs appear to disagree |
| 2026-08-24 | **Entity graph rebuilt** (`lib/seo-graph.ts`): one `@graph` per page, business + `WebSite` on every page from the layout, everything `@id`-linked to `#business`. Replaces the site-kit's unlinked standalone nodes. Adds `WebPage`/`AboutPage`/`ContactPage`/`CollectionPage`, `ItemList` on hubs, `Service` on service **and** city pages, `Article` on guides. Verified: 0 banned-markup hits, 35/35 FAQ answers match the rendered text verbatim | (this batch) | Entity resolution — brand search returning us, a knowledge panel, assistants naming the business correctly. Baseline: no knowledge panel, brand search surfaces competitors (2026-08-17) |
| 2026-08-24 | **`/guides/` silo opened** — hub + `pergola-permit` + `aluminum-vs-wood-pergola`, in the primary nav, two-way linked with `/service/pergolas/`. First coverage of the informational/commercial tier | (this batch) | GSC impressions on permit + comparison queries. **Baseline: zero** — the site had no page targeting them |
| 2026-08-24 | **Analytics events aligned to the spec**: `lead_submit` → `generate_lead`, added `form_start` (first focus, once per mount), `form_location` on all three events (`home-hero` / `contact-page`), `error_type` on failures, `consent` on leads. Container build spec: `docs/gtm-tag-spec.md` | (this batch) | Nothing yet — **the events still die in the dataLayer until the GTM tags exist**. This makes the site side ready, not the measurement |
| 2026-08-24 | `/services/` given its own intro + per-need routing; title → `עבודות אלומיניום לבית ולגינה`. **Deliberate exception to the 90-day title freeze**: this page was not in the query-led retitle tranche (`ade3f37`), so it had no fresh CTR baseline to protect. Its own 90-day clock starts today | (this batch) | Stops the hub duplicating the homepage; CTR on `עבודות אלומיניום` |
| 2026-08-24 | Two unsupportable claims removed from `/about/`: "הפכנו למובילים בתחום" and "מאות פרויקטים". Accordion display name → `סגירת מרפסות ותריסי אקורדיון` (slug and `<title>` unchanged — title stays frozen) | (this batch) | Removes live misleading-advertising exposure; recognition on `סגירת מרפסת` |
| 2026-08-24 | Assessed: `npm audit` 3 high (next/postcss/nanoid) — **all require a Next server this static export does not run**; 14.2.35 is already the newest 14.2.x and every fix needs a two-major jump. Decision recorded in `docs/security-posture.md`, not actioned | — | Prevents a needless Next 16 migration being re-proposed monthly |
| 2026-08-16 | Media catalog migration (images via imgquarry CDN) | `7a72e27` | LCP/bytes on gallery pages; CWV field data |
| 2026-08-17 | 9 verified defect fixes (placeholder markers, #1 claim, lead-form silent loss, 404 soft-404, footer cities, WhatsApp contrast, sibling links, city-page spacing) | `2bc8889` | lead_submit_failed visibility; a11y compliance; crawl of 4 orphan-ish cities |
| 2026-08-17 | Query-led titles (all ≤60), services in primary nav, sitemap lastmod removed + drift guard, mobile-menu a11y | `ade3f37` | GSC CTR on service/city queries (after deploy + 90-day freeze) |
| 2026-08-23 | DMARC added: `_dmarc` TXT `v=DMARC1; p=none; rua=mailto:yossi@skyshade.co.il; fo=1` (zone uses Cloudflare Email Routing; SPF + DKIM already present). Ramp to `quarantine` then `reject` after 4–6 weeks of clean reports — first identify the real outbound sender (likely Gmail send-as) and add its SPF/DKIM | (DNS via API) | Spoofing of yossi@ becomes visible in reports, then blocked |
| 2026-08-23 | www→apex 301 deployed as a zone Redirect Rule (template "Redirect from WWW to Root"); verified path + query preserved, http→https→apex chain = 2 hops | (dashboard) | GSC: www duplicates drop out of the index; one canonical host |
| 2026-08-23 | **All audit fixes went LIVE** — deploy pipeline rebuilt to push `out/` to the Cloudflare Pages project via wrangler-action on every push to main (secrets from the Sys Admin vault). Verified live: new titles, corrected permit FAQ in copy+schema, testimonials/stats removed, 404 fixed, services dropdown, consent checkbox, HSTS + 5 security headers + CSP-RO, immutable static cache, RSC noindex, legacy-photo 301s | `1d27af1` | From here on, GSC/GA4 deltas are attributable to dated entries |
| 2026-08-23 | Cloudflare: managed robots.txt off; AI Crawl Control set to Allow — GPTBot, OAI-SearchBot, ChatGPT-User, PerplexityBot, Perplexity-User, ClaudeBot, Claude-User, Claude-SearchBot, Google-Extended, meta-externalagent, Applebot-Extended, Amazonbot all verified 200 (were 403), incl. deep paths + sitemap | (dashboard) | AI-assistant referral sessions in GA4; citations in ChatGPT/Perplexity for brand + pergola queries (check monthly) |
| 2026-08-23 | Discovered live origin is Cloudflare Pages project `skyshade`, not GitHub Pages; headers/redirects moved into `public/_headers` + `public/_redirects` | `e0f2d9e` | Security headers + HSTS observable once Pages deploys |
| 2026-08-17 | Stop-ship legal batch: corrected permit FAQ (copy+schema), truthful privacy policy, testimonials unrendered, verifiable trust stats, footer contrast /85 & /80, consent checkbox + privacy line, GTM→head, first-touch capture, data-cta labels, monthly rebuild cron, framer-motion removed, .htaccess deleted | (this commit) | Removes live legal exposure; enables lawful nurture; GTM fires earlier |
